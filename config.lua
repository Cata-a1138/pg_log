Config = {
    -- 框架 (auto, esx, qb, custom)
    Framework = 'auto',

    -- 记录方式 (ox_lib, pg_imbot.kook)
    Log = false,

    -- 伤害日志
    WeaponDamage = true,

    -- 聊天信息
    ChatMessage = true,

    -- 加入退出
    PlayerJoinQuit = true,
}
